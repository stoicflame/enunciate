1. Update the documentation to reflect any changes.
    1a. Make sure you've updated the insert sidebox on the index page.
    1b. Make sure you've updated the downloads page to reflect where the new files will be.
    1c. Make sure you've also updated the version in the executables.html page.
    1d. Make sure you're also updated the version maven plugin in the sample poms.
2. Make sure any new modules are included
    2a. Make sure the parent pom includes any new modules.
    2b. Make sure the top pom includes any new modules.
    2c. Make sure the rt pom includes any new modules.
3. "mvn release:prepare" maybe "mvn release:branch" just to create the branch?
4. "mvn release:perform"

(Your new version should now be deployed.)

5. Build the distribution (e.g. "ant -f build-site.xml dist")
6. mount the dist repo (e.g. "mount /home/heatonra/mnt/enunciate/dist")
7. mount the web repo (e.g. "mount /home/heatonra/mnt/enunciate/web")
8. Copy the dist files to the dist repo (e.g. "cp -v target/*.zip target/*.tar.gz /home/heatonra/mnt/enunciate/dist")
    8a. Make sure the links you have on the downloads page are correct.
9. Copy the web content to the web repo (e.g. "rsync -av --delete target/docs/ /home/heatonra/mnt/enunciate/web")
10. Make your announcements.
    10a. Codehaus news
    10b. Java.net
    10c. Webcohesion?
    10d. Mailing lists.
    10e. Serverside.com?
    10f. Javalobby?
11. Update the WIKI, close all JIRA issues for last version.  Announce version release in JIRA.
