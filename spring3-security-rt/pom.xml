<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.enunciate</groupId>
    <artifactId>enunciate-parent</artifactId>
    <version>1.21-SNAPSHOT</version>
  </parent>

  <artifactId>enunciate-spring3-security-rt</artifactId>
  <packaging>pom</packaging>
  <name>Enunciate - Spring 3 Security Runtime</name>
  <description>A pom for bundling all the (optional) dependencies needed to apply spring 3 security.</description>

  <properties>
    <spring.version>3.0.2.RELEASE</spring.version>
    <spring.security.version>3.0.2.RELEASE</spring.security.version>
    <spring.security.oauth.version>3.16.SS3</spring.security.oauth.version>
  </properties>

  <dependencies>
    <!-- ============================================= -->
    <!--Spring Security Dependencies (all are optional)-->
    <!-- ============================================= -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-tx</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-core</artifactId>
      <version>${spring.security.version}</version>
    </dependency>

    <dependency>
      <!-- declaring this dep to override spring-security-openid's dep -->
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-openid</artifactId>
    </dependency>

    <dependency>
      <groupId>org.codehaus.spring-security-oauth</groupId>
      <artifactId>spring-security-oauth</artifactId>
    </dependency>
  </dependencies>

</project>
