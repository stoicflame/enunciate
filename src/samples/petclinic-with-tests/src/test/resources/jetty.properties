#
# Jetty integration test configuration - must be filtered by Maven:
#
context=/${project.build.finalName}
webapp.home=${project.build.directory}/${project.build.finalName}
host=localhost
protocol=http
port=0
