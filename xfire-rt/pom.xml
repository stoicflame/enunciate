<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.codehaus.enunciate</groupId>
    <artifactId>enunciate-parent</artifactId>
    <version>1.20-RC1</version>
  </parent>

  <artifactId>enunciate-xfire-rt</artifactId>
  <name>Enunciate - XFire Runtime</name>
  <description>The Enunciate XFire runtime libraries support the XFire-backed SOAP endpoints.</description>

  <dependencies>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>enunciate-spring-app-rt</artifactId>
      <version>${project.version}</version>
      <exclusions>
        <exclusion>
          <!--disable the jaxws-rt dependencies, since those are the default-->
          <groupId>${project.groupId}</groupId>
          <artifactId>enunciate-spring-jaxws-rt</artifactId>
        </exclusion>
        <exclusion>
          <!--disable the jaxws-rt dependencies, since those are the default-->
          <groupId>${project.groupId}</groupId>
          <artifactId>enunciate-jaxws-ri-rt</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>

      <scope>provided</scope>      
    </dependency>

    <dependency>
      <groupId>org.jdom</groupId>
      <artifactId>jdom</artifactId>
    </dependency>

    <dependency>
      <groupId>javax.mail</groupId>
      <artifactId>mail</artifactId>
    </dependency>

    <dependency>
      <groupId>org.codehaus.woodstox</groupId>
      <artifactId>wstx-asl</artifactId>
    </dependency>

    <dependency>
      <groupId>wsdl4j</groupId>
      <artifactId>wsdl4j</artifactId>
    </dependency>

    <dependency>
      <groupId>org.codehaus.xfire</groupId>
      <artifactId>xfire-all</artifactId>
    </dependency>

  </dependencies>

</project>